# ExUnit.start()

# # Start the Ecto test repo when ecto_sqlite3 is available (used by mix test.ecto)
# if Code.ensure_loaded?(Ecto.Adapters.SQLite3) do
#   {:ok, _} = SferaDoc.TestRepo.start_link()
#   Ecto.Adapters.SQL.Sandbox.mode(SferaDoc.TestRepo, :manual)
# end
